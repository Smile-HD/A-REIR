# ============================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ============================================
# Instrucciones:
# 1. Instala PostgreSQL en tu PC si no lo tienes
# 2. Crea la base de datos (ver instrucciones abajo)
# 3. Copia este archivo y renómbralo como .env
# 4. Configura los valores según tu entorno local
# 5. Ejecuta: npx prisma migrate deploy
# 6. (Opcional) Ejecuta: npm run seed
# 7. NUNCA subas el archivo .env a GitHub (ya está en .gitignore)

# ============================================
# BASE DE DATOS
# ============================================
# PASO 1: Crear la base de datos en PostgreSQL
# Abre psql o pgAdmin y ejecuta:
#   CREATE DATABASE taller;
#
# PASO 2: Configura la URL de conexión
# Formato: postgres://USUARIO:CONTRASEÑA@HOST:PUERTO/NOMBRE_BD
# 
# Ejemplo con usuario postgres y contraseña 'mipassword':
DATABASE_URL="postgres://postgres:mipassword@localhost:5432/taller"
#
# Reemplaza 'mipassword' con tu contraseña de PostgreSQL

# ============================================
# SERVIDOR
# ============================================
# Puerto donde correrá el servidor backend (por defecto 3000)
PORT=3000

# ============================================
# AUTENTICACIÓN JWT
# ============================================
# Genera una clave secreta fuerte y única para tu aplicación
# Puedes generarla con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_clave_secreta_super_segura_cambiala_aqui
JWT_EXPIRES_IN=7d
